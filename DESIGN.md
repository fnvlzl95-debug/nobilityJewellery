---
name: 귀족 Noblesse
description: 종로 귀금속 도매점의 다크 럭셔리 웹 — Noir Opulence
colors:
  black: "#0a0a0a"
  black-light: "#111111"
  black-lighter: "#1a1a1a"
  gold: "#c9a227"
  gold-light: "#d4b44a"
  gold-dark: "#a68820"
  champagne: "#f5e6cc"
  white: "#fafafa"
  gray: "#888888"
  gray-dark: "#444444"
  gray-darker: "#2a2a2a"
  surface-raised: "rgba(250, 250, 250, 0.02)"
  hairline: "rgba(250, 250, 250, 0.08)"
  gold-hairline: "rgba(201, 162, 39, 0.35)"
  gold-wash: "rgba(201, 162, 39, 0.1)"
  scrim: "rgba(10, 10, 10, 0.62)"
  scrim-soft: "rgba(10, 10, 10, 0.3)"
  black-warm: "#1a1510"
  gold-label: "#120e06"
  champagne-highlight: "rgba(255, 249, 232, 0.85)"
  champagne-hairline: "rgba(255, 243, 210, 0.5)"
  caution: "#ffb4b4"
  caution-hairline: "rgba(255, 168, 168, 0.6)"
  caution-hairline-soft: "rgba(255, 168, 168, 0.18)"
  caution-wash: "rgba(255, 168, 168, 0.05)"
  kakao-yellow: "#FEE500"
  kakao-label: "#3C1E1E"
typography:
  numeral-hero:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "clamp(120px, 25vw, 200px)"
    fontWeight: 300
    lineHeight: 1
    letterSpacing: "normal"
  numeral-watermark:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "clamp(100px, 20vw, 200px)"
    fontWeight: 300
    lineHeight: 1
    letterSpacing: "normal"
  display-statement:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "clamp(48px, 8vw, 80px)"
    fontWeight: 300
    lineHeight: 1.05
    letterSpacing: "-0.02em"
  display-xl:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "clamp(36px, 6vw, 56px)"
    fontWeight: 300
    lineHeight: 1.15
    letterSpacing: "-0.02em"
  display-contact:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "clamp(36px, 6vw, 52px)"
    fontWeight: 300
    lineHeight: 1.15
    letterSpacing: "-0.02em"
  display-lg:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "clamp(32px, 5vw, 48px)"
    fontWeight: 300
    lineHeight: 1.2
    letterSpacing: "-0.01em"
  display-md:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "clamp(36px, 4vw, 48px)"
    fontWeight: 300
    lineHeight: 1.2
    letterSpacing: "-0.01em"
  display-faq:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "clamp(32px, 5vw, 44px)"
    fontWeight: 300
    lineHeight: 1.2
    letterSpacing: "-0.01em"
  display:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "clamp(30px, 4vw, 44px)"
    fontWeight: 600
    lineHeight: 1.25
    letterSpacing: "-0.01em"
  display-sub:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "clamp(28px, 5vw, 40px)"
    fontWeight: 400
    lineHeight: 1.25
    letterSpacing: "-0.01em"
  display-error:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "clamp(24px, 5vw, 36px)"
    fontWeight: 300
    lineHeight: 1.3
    letterSpacing: "0.05em"
  display-section:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "clamp(24px, 3vw, 32px)"
    fontWeight: 400
    lineHeight: 1.3
    letterSpacing: "-0.01em"
  title:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "clamp(26px, 3vw, 38px)"
    fontWeight: 600
    lineHeight: 1.25
    letterSpacing: "-0.01em"
  title-guide:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "clamp(22px, 4vw, 30px)"
    fontWeight: 600
    lineHeight: 1.35
    letterSpacing: "-0.01em"
  title-sub:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "clamp(16px, 2vw, 22px)"
    fontWeight: 400
    lineHeight: 1.6
    letterSpacing: "normal"
  heading-xl:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "36px"
    fontWeight: 300
    lineHeight: 1.2
    letterSpacing: "-0.01em"
  heading-lg:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "32px"
    fontWeight: 300
    lineHeight: 1.25
    letterSpacing: "-0.01em"
  heading:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "28px"
    fontWeight: 600
    lineHeight: 1.3
    letterSpacing: "-0.01em"
  subheading:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "24px"
    fontWeight: 600
    lineHeight: 1.35
    letterSpacing: "-0.01em"
  section:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "22px"
    fontWeight: 600
    lineHeight: 1.4
    letterSpacing: "-0.01em"
  section-sm:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "20px"
    fontWeight: 600
    lineHeight: 1.4
    letterSpacing: "-0.01em"
  lead:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "18px"
    fontWeight: 400
    lineHeight: 1.7
    letterSpacing: "normal"
  reading:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "17px"
    fontWeight: 400
    lineHeight: 1.8
    letterSpacing: "normal"
  body-lg:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "16px"
    fontWeight: 400
    lineHeight: 1.7
    letterSpacing: "normal"
  body:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "15px"
    fontWeight: 400
    lineHeight: 1.6
    letterSpacing: "normal"
  ui:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "14px"
    fontWeight: 400
    lineHeight: 1.5
    letterSpacing: "normal"
  meta:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "13px"
    fontWeight: 400
    lineHeight: 1.5
    letterSpacing: "0.16em"
  mono:
    fontFamily: "ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace"
    fontSize: "13px"
    fontWeight: 400
    lineHeight: 1.5
    letterSpacing: "0.04em"
  fine:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "12px"
    fontWeight: 400
    lineHeight: 1.5
    letterSpacing: "0.04em"
  eyebrow:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, Apple SD Gothic Neo, Malgun Gothic, sans-serif"
    fontSize: "11px"
    fontWeight: 500
    lineHeight: 1.4
    letterSpacing: "0.2em"
rounded:
  none: "0px"
spacing:
  xs: "6px"
  sm: "10px"
  md: "20px"
  lg: "40px"
  xl: "80px"
  section: "clamp(80px, 12vh, 160px)"
  gutter: "clamp(24px, 5vw, 80px)"
components:
  surface-card:
    backgroundColor: "{colors.surface-raised}"
    textColor: "{colors.white}"
    rounded: "{rounded.none}"
    padding: "16px"
  button-primary:
    backgroundColor: "{colors.gold}"
    textColor: "{colors.black}"
    typography: "{typography.body}"
    rounded: "{rounded.none}"
    padding: "0 18px"
    height: "48px"
  button-primary-hover:
    backgroundColor: "{colors.gold-light}"
    textColor: "{colors.black}"
  button-ghost:
    backgroundColor: "transparent"
    textColor: "{colors.gold}"
    typography: "{typography.meta}"
    rounded: "{rounded.none}"
    padding: "10px 14px"
  button-ghost-hover:
    backgroundColor: "{colors.gold-wash}"
    textColor: "{colors.gold-light}"
  chip-meta:
    backgroundColor: "{colors.surface-raised}"
    textColor: "{colors.gray}"
    typography: "{typography.fine}"
    rounded: "{rounded.none}"
    padding: "6px 10px"
---

# 귀족 Noblesse — Noir Opulence

## Overview

거의 검정에 가까운 배경 위에 금색 한 가지만 강조색으로 쓰는 다크 럭셔리 시스템이다. 화면의 유일한 채도는 **제품 사진과 골드 액센트**이고, UI는 그 뒤로 물러난다. 둥근 모서리를 쓰지 않는다 — 모든 면과 버튼이 직각이고, 구분은 1px 헤어라인으로만 한다. 이 절제가 이 사이트의 성격이다.

주 사용자는 모바일로 검색해 들어온 개인 고객이고, 모든 화면의 종착지는 **카카오톡 상담**이다. 정보성 가이드 → 갤러리 → 상담으로 이어지는 흐름을 방해하지 않는 것이 최우선이다.

## Colors

- 바닥은 `black` `#0a0a0a`. 카드/입력면처럼 살짝 떠 있는 면은 배경색을 바꾸지 않고 `surface-raised`(흰색 2% 알파)를 덮는다. 이미지 자리표시자만 `black-light` `#111111`.
- `gold` `#c9a227`은 **행동과 강조 전용**이다. 본문 텍스트에 쓰지 않는다. 호버 시 `gold-light` `#d4b44a`로 올린다.
- 텍스트 위계는 `white` `#fafafa` → `gray` `#888888` 두 단계면 충분하다. `gray-dark`는 비활성·구분선급에만.
- `champagne` `#f5e6cc`는 골드보다 부드러운 강조가 필요할 때만 쓰는 예비 색이다.
- 경계선은 색이 아니라 알파다: 중립 `hairline`(흰색 8%), 골드 강조 `gold-hairline`(골드 35%).
- 사진 위에 UI를 얹을 때만 `scrim`(`rgba(10, 10, 10, 0.62)`)을 쓴다. 순수 검정이 아니라 `black` `#0a0a0a` 기반이라 페이지 바닥과 같은 온도로 가라앉는다. 카드의 `+N` 배지와 상세 페이지 확대 버튼이 이걸 공유한다. 사진 위가 아닌 곳에는 쓰지 않는다 — 떠 있는 면은 `surface-raised`다. 그림자·텍스트 그림자처럼 옅게 깔 때는 `scrim-soft`(30%).
- **순수 검정(`#000`)을 쓰지 않는다.** 이 사이트의 검정은 전부 `#0a0a0a` 계열이다. `rgba(0,0,0,…)`이 보이면 `rgba(10,10,10,…)`으로 바꾼다.
- 포커스 링은 전역 단일 정책 — `outline: 2px solid var(--gold); outline-offset: 2px`. 재정의 금지.

### 브랜드 고정색 (변경 금지)

- `kakao-yellow` `#FEE500` + `kakao-label` `#3C1E1E` — 카카오톡 버튼 전용. **카카오 브랜드 가이드가 지정한 값이라 톤을 맞추려고 건드리면 안 된다.** 사이트 전체에서 이 조합 하나만 쓴다(예전에 `#2f1b12`/`#fee500`으로 갈려 있던 걸 통일했다).
- 골드 그라디언트 CTA(모바일 전환 바)만 자체 팔레트를 쓴다: 라벨 `gold-label` `#120e06`, 테두리 `champagne-hairline`, 호버 테두리 `champagne-highlight`. 금색 배경 위에서는 흰 글자가 읽히지 않아 어두운 따뜻한 색이 필요하다.
- `black-warm` `#1a1510`은 랜딩 CTA 배경 그라디언트의 중간 정지점 전용이다.

### 의미색

- `caution` `#ffb4b4` — 가이드 아티클의 주의 콜아웃(`.guide-caution`) 전용. 테두리는 `caution-hairline`(60%)·`caution-hairline-soft`(18%), 배경은 `caution-wash`(5%). 이 시스템에서 유일하게 골드가 아닌 강조색이므로 다른 곳으로 번지지 않게 한다.

## Typography

Pretendard Variable 한 서체로 전부 처리한다. 세리프를 섞지 않는다 — 위계는 **크기·굵기·자간**으로만 만든다.

타입은 두 층으로 갈린다. **UI 텍스트는 고정 px 램프**, **제목은 `clamp()` 유동 스케일**이다. 새 값을 만들기 전에 이미 있는 단계를 먼저 찾는다.

**고정 UI 램프** — 11 · 12 · 13 · 14 · 15 · 16 · 17 · 18 · 20 · 22 · 24 · 28 · 32 · 36px

**유동 디스플레이** — 히어로·섹션 제목은 전부 `clamp(min, vw, max)`다. 이미 정의된 것만 쓴다:

| 토큰 | 값 | 쓰이는 곳 |
|---|---|---|
| `display-statement` | `clamp(48px, 8vw, 80px)` | 랜딩 최종 CTA 문구 |
| `display-xl` | `clamp(36px, 6vw, 56px)` | 랜딩 히어로 |
| `display-lg` | `clamp(32px, 5vw, 48px)` | **표준 페이지 히어로** — 8개 페이지가 공유한다 |
| `display` | `clamp(30px, 4vw, 44px)` | 갤러리 제목 |
| `display-sub` | `clamp(28px, 5vw, 40px)` | 가이드 아티클 제목 |
| `title` | `clamp(26px, 3vw, 38px)` | 제품 상세 제목 |
| `title-guide` | `clamp(22px, 4vw, 30px)` | 가이드 본문 h2 |
| `numeral-hero` / `numeral-watermark` | `clamp(120px, 25vw, 200px)` / `clamp(100px, 20vw, 200px)` | 404 숫자, 히어로 배경 숫자 |

새 페이지의 히어로는 `display-lg`를 그대로 쓴다. 페이지마다 clamp 값을 새로 지어내지 않는다.

- `heading` 28px · `subheading` 24px · `section` 22px · `section-sm` 20px — 섹션 제목 4단계.
- `lead` 18px 도입 문단, `reading` 17px는 **가이드 아티클 본문 전용**(장문 한글 가독성 때문에 UI 본문보다 크다), `body-lg` 16px 상세 본문, `body` 15px 기본 본문.
- `mono` 13px는 문의 접수번호처럼 **식별자·수치**에만 쓴다. '기술적으로 보이려고' 쓰지 않는다.
- `ui` 14px는 이 코드베이스에서 가장 많이 쓰는 크기다(90곳). 버튼·라벨·정의목록 값처럼 화면을 채우는 UI 텍스트의 기본값.
- `meta` 13px + 0.16em 자간은 수량·상태·영문 부제. `fine` 12px는 칩·캡션.
- `eyebrow` 11px / 0.2em는 기존 화면(`.gallery-label`, `.category-label`)에 남아 있는 라벨 스타일이다. **신규 화면에는 새로 도입하지 않는다** — 제목이 스스로 서게 두고, 위치 정보가 필요하면 breadcrumb를 쓴다.
- 한글 조판 규칙이 전역에 걸려 있다: `word-break: keep-all`(어절 단위 줄바꿈), 제목 `text-wrap: balance`, 본문 `text-wrap: pretty`. 새 컴포넌트에서 이걸 덮어쓰지 않는다.

## Layout

- 콘텐츠 최대폭 1400px, 좌우 패딩 40px(모바일 20px), 하단 120px.
- 세로 리듬은 `--section-padding: clamp(80px, 12vh, 160px)`, 가로 여백은 `--gutter: clamp(24px, 5vw, 80px)`.
- 제품 그리드는 데스크톱 4열 / 태블릿 2열 / 모바일 1열, gap 20px.
- 제품 카드 이미지는 `aspect-ratio: 1` + `object-fit: cover`. 원본 종횡비가 달라도 카드 격자가 흐트러지지 않는다.
- 스크롤은 Lenis 스무스 스크롤. `prefers-reduced-motion: reduce`면 Lenis를 아예 초기화하지 않고 네이티브 스크롤 + 스크롤 리스너로 폴백한다.

## Elevation & Depth

그림자를 쓰지 않는다. 깊이는 세 가지로만 표현한다:

1. `surface-raised` 알파 오버레이 — 면이 떠 있음
2. 1px `hairline` — 면의 경계
3. 호버 시 골드 보더 + `gold-wash` 배경 — 상호작용 가능

라이트박스만 예외로 전체 화면 어두운 오버레이를 쓴다.

## Shapes

**모서리 반경 0.** 버튼, 카드, 칩, 입력창 전부 직각이다. 원형은 아이콘 버튼에도 쓰지 않는다. 이건 취향이 아니라 이 시스템의 규칙이다.

아이콘은 24×24 인라인 SVG, `stroke-width: 1`~`1.5`, `fill: none`, `stroke: currentColor`. 아이콘 라이브러리를 추가하지 않는다.

## Components

- **surface-card** — `surface-raised` 배경 + `hairline` 보더. 호버 시 보더를 `gold-hairline`으로. 제품 카드, 안내 카드 공통.
- **button-primary** — 골드 배경 + 검정 글자, 높이 48px. 화면당 하나. 상세 페이지에서는 카카오톡 상담이 이 자리다.
- **button-ghost** — 투명 배경 + 골드 글자 + 골드 헤어라인. 보조 경로(전화, 문의 폼, 카테고리 이동).
- **chip-meta** — 재질·제작 기간처럼 짧은 사실을 나열하는 12px 칩.
- **lightbox** — 전체화면 오버레이, 좌우 화살표, 인디케이터, `Escape`/`←`/`→` 키 지원, 열 때 닫기 버튼으로 포커스 이동하고 닫을 때 트리거로 되돌린다. `body.style.overflow = 'hidden'` + `lenis.stop()`을 짝으로 건다.
- **reveal** — `IntersectionObserver`로 `.revealed` 클래스를 붙이는 진입 애니메이션. `js-enabled` 게이트 클래스가 있어 JS 미실행 시 콘텐츠가 숨지 않는다.

## Do's and Don'ts

**Do**

- 강조가 필요하면 골드를 쓰되, 한 화면에 primary 버튼은 하나만.
- 새 CTA에는 `useGtag()`의 `trackKakaoClick` / `trackPageInquiryClick` / `trackEvent` / `trackMetaEvent`를 붙인다. 계측 없는 전환 버튼을 만들지 않는다.
- `<img>`에는 항상 서술형 `alt`를 넣는다. 제품 이미지는 `gallery-items.ts`의 `imageAlts`를 쓰고, 제목만 넣지 않는다.
- 첫 화면 이미지는 `loading="eager"`, 나머지는 `loading="lazy"`.
- 색·간격·서체는 `assets/css/main.css`의 CSS 변수에서 가져온다. Tailwind 팔레트는 그 값을 참조만 하는 거울이다.

**Don't**

- 모서리를 둥글게 하지 않는다. `border-radius`를 새로 도입하지 않는다.
- `box-shadow`로 깊이를 만들지 않는다.
- 본문 텍스트를 골드로 칠하지 않는다. 골드는 행동의 색이다.
- **그라디언트 텍스트(`background-clip: text`)를 쓰지 않는다.** 강조는 크기와 굵기로 만든다.
- 12px 미만 글자를 쓰지 않는다. 유일한 예외가 `eyebrow` 11px인데, 대문자 라틴 + 0.2em 자간이라 명목 크기보다 크게 읽히는 경우에만이다. 한글에는 쓰지 않는다.
- 레이아웃 속성(`width`·`height`·`top`·`left`)을 트랜지션하지 않는다. `transform`과 `opacity`만 애니메이션한다.
- **가격·중량·석수 같은 수치를 화면에 적지 않는다.** 금시세 연동이라 상담에서만 안내한다 (`PRODUCT.md` 참조).
- 세리프나 장식 서체를 섞지 않는다. Pretendard 하나로 끝낸다.
- 새 아이콘/애니메이션 라이브러리를 추가하지 않는다. Lenis와 인라인 SVG로 충분하다.
